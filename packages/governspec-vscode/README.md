# GovernSpec VS Code Extension

This package contains the VS Code extension MVP for GovernSpec.

The MVP focuses on:

- validating the current file
- inspecting normalized output
- compiling the current file to supported targets
- testing generated output files
- showing an MCP setup snippet for `governspec-mcp`

It intentionally does not add a chat UI, provider integration, or a Copilot-specific target.
